[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'celery -A app.workers.celery_app.celery_app worker --queues=bulk,webhooks --concurrency=2 --loglevel=info'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
